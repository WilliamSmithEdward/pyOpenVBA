"""In-memory Office applications for the VBA interpreter to move.

Each host here is the object model a macro sees, holding real state:
:mod:`pyopenvba.apps.excel` first, with Word, PowerPoint and Access to
follow the same shape.
"""
